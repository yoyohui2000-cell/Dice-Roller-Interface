[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --filter @workspace/api-server..."]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server build"]

[start]
cmd = "pnpm --filter @workspace/api-server start"
